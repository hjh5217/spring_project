<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org/DTD Mapper3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="aaa.service.MybatisMapper">
	
	<select id="list" resultType="bDTO">
		select * from board order by gid desc, seq
	</select>
	
	<select id="listSch" resultType="bDTO" parameterType="bDTO">
		select * from board
		<where>
			<if test="title!=null"> 
			title like concat('%',#{title},'%')
			</if>
			<if test="pname!=null"> 
			and pname like concat('%',#{pname},'%')
			</if>
		</where>
	</select>
	
	<select id="cntSch" resultType="bDTO" parameterType="bDTO">
		select * from board
		<where>
			<if test="grade!=null"> 
				<choose>
					<when test="grade == 'high'">
						cnt > 10
					</when>
					<when test="grade == 'middle'">
						cnt > 5 and cnt &lt;=10
					</when>
					<otherwise>
						cnt &lt;= 5
					</otherwise>
				</choose>
			</if>
		</where>
	</select>
	
	<select id="listCnt" resultType="bDTO">
		select * from board order by gid desc, seq
	</select>

	<select id="detail" resultType="bDTO" parameterType="Integer">
		select * from board where id = #{id}
	</select>
	
	<select id="totalCnt" resultType="Integer">
		select count(*) from board
	</select>
	
	<select id="getMaxId" resultType="Integer">
		select max(id) from board;
	</select>
	
	<select id="idPwChk" resultType="Integer">
		select * from board where id = #{id} and pw = #{pw}
	</select>
	
	<insert id="insert" parameterType="bDTO">
		insert into board (title, pname, pw, upfile, content, reg_date, cnt, seq, lev , gid)
		values
		(#{title}, #{pname}, #{pw}, #{upfile}, #{content}, sysdate(), 0, 0, 0, #{gid})
	</insert>
	
	<delete id="delete" parameterType="bDTO">
		delete from board where id = #{id} and pw = #{pw}
	</delete>
	
	<delete id="fileDelete" parameterType="bDTO">
		update board set upfile = null where id = #{id} and pw = #{pw}
	</delete>
	
	<update id="update" parameterType="bDTO">
		update board 
		set pname = #{pname}, content = #{content}, title = #{title}, upfile = #{upfile}
		where id = #{id} and pw = #{pw}
	</update>
	
	<update id="addcnt" parameterType="Integer">
		update board set cnt = cnt + 1 where id = #{id}
	</update>
	
	
</mapper>