package aop_p.adv;

import org.aspectj.lang.JoinPoint;

public class FigureAdvice {

	final double PI = Math.PI;
	int area = 0;
	int perimeter = 0;
	
	void rectangle(JoinPoint joinPoint) {
		int width = joinPoint.getArgs()[0];
		if(joinPoint.getArgs().length>=2) {
			area = 
			System.out.println();
		}
	}
}
